export type TemaType = {
  id?: string;
  nome: string;
  slug: string;
  descricao: string;
};

export type AssuntoType = {
  categoria: string;
  nome: string;
  descricao: string;
  posicionamento: string;
};
